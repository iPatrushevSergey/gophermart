# Secrets and environment-specific overrides only.
# Non-secret baseline config is in app/configs/gophermart.yaml.

# Required secrets
DATABASE_URI=postgres://user:password@localhost:5432/gophermart?sslmode=disable
JWT_SECRET=change-me-in-production

# Optional per-environment overrides
# LOG_LEVEL=debug
# RUN_ADDRESS=0.0.0.0:8080
